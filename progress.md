2026-07-15
SQL: Day 1. Neon + DBeaver connected (SSL). Loaded clinical
     trial DB. 8 queries — SELECT, WHERE, BETWEEN, ORDER BY,
     LIMIT, DISTINCT. WHERE = yes/no test per row.
     > vs >= matters. DISTINCT = unique values.
Stats: shifted to tomorrow (slept off sleep debt).
Push: yes 